{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "append_prompt": {
      "title": "Append Prompt",
      "description": "Extra text appended to the prompt",
      "type": [
        "string",
        "null"
      ],
      "format": "textarea",
      "default": null
    },
    "model": {
      "type": [
        "string",
        "null"
      ]
    },
    "sandbox": {
      "type": [
        "boolean",
        "null"
      ]
    },
    "sandbox_type": {
      "description": "Sandbox backend/type. Examples: docker, podman, sandbox-exec. Sets BOB_SHELL_SANDBOX.",
      "type": [
        "string",
        "null"
      ]
    },
    "seatbelt_profile": {
      "description": "macOS Seatbelt profile. Sets SEATBELT_PROFILE.",
      "type": [
        "string",
        "null"
      ]
    },
    "sandbox_flags": {
      "description": "Extra container sandbox flags. Sets SANDBOX_FLAGS.",
      "type": [
        "string",
        "null"
      ]
    },
    "sandbox_set_uid_gid": {
      "description": "Override Linux UID/GID mapping behavior for container sandboxing. Sets SANDBOX_SET_UID_GID.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "trust": {
      "description": "Trust the current Bob workspace.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "instance_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "team_id": {
      "type": [
        "string",
        "null"
      ]
    },
    "max_coins": {
      "description": "Maximum Bob budget/coins for this run.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0
    },
    "pre_check_auto_approved": {
      "description": "Pre-check whether auto-approved commands are safe to run.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "allowed_tools": {
      "description": "Tools Bob may run without confirmation.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "allowed_mcp_server_names": {
      "description": "Allowed MCP server names for Bob.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "include_directories": {
      "description": "Extra directories Bob should include in the workspace.",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "approval_mode": {
      "description": "Bob approval mode: default, auto_edit, or yolo.",
      "type": [
        "string",
        "null"
      ]
    },
    "chat_mode": {
      "description": "Bob chat mode slug. Supports built-in modes (advanced, code, ask, plan) and custom mode slugs.",
      "type": [
        "string",
        "null"
      ]
    },
    "hide_intermediary_output": {
      "description": "Hide intermediary output and show only final completion output.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "base_command_override": {
      "title": "Base Command Override",
      "description": "Override the base command with a custom command",
      "type": [
        "string",
        "null"
      ]
    },
    "additional_params": {
      "title": "Additional Parameters",
      "description": "Additional parameters to append to the base command",
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "string"
      }
    },
    "env": {
      "title": "Environment Variables",
      "description": "Environment variables to set when running the executor",
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "type": "object"
}